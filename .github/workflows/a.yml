on:
  workflow_dispatch:
  schedule:
    - cron: '45 */168 * * *'


jobs:

  cron:
    runs-on: ubuntu-latest

    steps:
      - uses: fu1ny2t-3es/be1tl2-pc3-li4re5ro@cron
        with:
          token: ${{ secrets.token }}
